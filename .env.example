# Copia este archivo como ".env" (sin ".example") y pon tu clave real ahí.
# El archivo ".env" nunca se sube a git (ya está en .gitignore).
# Opcional: GEMINI_MODEL para cambiar de modelo sin tocar código.

GEMINI_API_KEY=tu_clave_de_gemini_aqui
